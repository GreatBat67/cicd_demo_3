
DEFINE TABLE CICD_DEMO_DEV.PROJECTS.PATIENT_VISITS (
    VISIT_ID VARCHAR NOT NULL,
    PATIENT_ID VARCHAR NOT NULL,
    VISIT_DATE VARCHAR,
    DOCTOR_NAME VARCHAR,
    VISIT_TYPE VARCHAR,
    DIAGNOSIS VARCHAR
);